import DefaultLayout from "../components/DefaultLayout"

const AddProducts = () => {
    return (
        <DefaultLayout>
            Add New Product
        </DefaultLayout>
    )
}

export default AddProducts
